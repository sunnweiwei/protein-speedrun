{
  "schema_version": 1,
  "candidate_id": "tiny_mlm_smoke",
  "seed": 42,
  "objective": "mlm",
  "model": {
    "vocab_size": 23,
    "max_length": 96,
    "d_model": 32,
    "num_layers": 2,
    "num_heads": 4,
    "ffn_multiplier": 2,
    "dropout": 0.0,
    "attention_mode": "bidirectional"
  },
  "training": {
    "device": "cuda",
    "steps": 2,
    "checkpoint_every": 1,
    "batch_size": 8,
    "max_length": 96,
    "mask_probability": 0.15,
    "learning_rate": 0.002,
    "weight_decay": 0.01,
    "grad_clip_norm": 1.0,
    "optimizer": "adamw"
  },
  "evaluation": {
    "device": "cuda",
    "target_contact_p_at_l": null,
    "consecutive_checkpoint_passes": 2
  }
}
